<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Álgebrabot</title>
    
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 20px;
        }

        h1 {
            color: #2c3e50;
        }

        img {
            max-width: 100%;
            height: auto;
            margin-top: 20px;
        }
    </style>
</head>
<body>

    <h1>Bienvenido a ÁLGEBRABOT</h1>
    <p>Interactúa con el chatbot utilizando el widget a continuación:</p>

    <!-- Asegúrate de que la imagen está en la misma carpeta o usa una URL completa -->
    <img id="bot-image" src="IMAGEN.webp" alt="Algebrabot">
    
    <p id="image-status"></p>

    <!-- Script del chatbot de Voiceflow -->
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            console.log("📢 Intentando cargar el chatbot...");

            try {
                var v = document.createElement("script");
                v.src = "https://cdn.voiceflow.com/widget-next/bundle.mjs";
                v.type = "text/javascript";
                v.async = true;

                v.onload = function () {
                    if (window.voiceflow && window.voiceflow.chat) {
                        window.voiceflow.chat.load({
                            verify: { projectID: "679b9449856ad419ecfaabba" },
                            url: "https://general-runtime.voiceflow.com",
                            versionID: "production"
                        });
                        console.log("✅ Chatbot cargado correctamente.");
                    } else {
                        console.error("⚠️ Error: Voiceflow no está definido correctamente.");
                    }
                };

                v.onerror = function () {
                    console.error("⚠️ Error al cargar el script de Voiceflow.");
                };

                document.body.appendChild(v);
            } catch (error) {
                console.error("⚠️ Error inesperado al cargar el chatbot:", error);
            }
        });

        // Verificación de carga de la imagen
        window.onload = function () {
            var img = document.getElementById("bot-image");
            img.onerror = function () {
                document.getElementById("image-status").innerHTML = "⚠️ Imagen no encontrada. Verifica la ruta.";
                console.error("⚠️ Imagen no cargada. Revisa la ruta del archivo.");
            };
            img.onload = function () {
                document.getElementById("image-status").innerHTML = "✅ Imagen cargada correctamente.";
                console.log("✅ Imagen cargada correctamente.");
            };
        };
    </script>

    <!-- Script para mantener la pestaña activa -->
    <script>
        function keepAlive() {
            console.log("🔄 Manteniendo la pestaña activa:", new Date().toLocaleTimeString());
        }

        document.addEventListener("visibilitychange", function () {
            if (!document.hidden) {
                keepAlive();
                setInterval(keepAlive, 300000);
            }
        });
    </script>

</body>
</html>

